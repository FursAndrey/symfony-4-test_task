{% extends 'base.html.twig' %}

{% block body %}
    <p>
        <a href="{{ path('login') }}" class="btn btn-info">{{ login }}</a>
        <a href="{{ path('user_registration') }}" class="btn btn-success">{{ register }}</a>
    </p>
{% endblock %}