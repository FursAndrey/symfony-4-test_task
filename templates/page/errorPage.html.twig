{% extends 'base.html.twig' %}

{% block body %}
    <h2 class="text-danger p-3">{{ error }}</h2>
    <p class="p-3 h5">{{ descriptionError }}</p>
    <p><a href="{{ path('user_page') }}" class="text-success p-3 h5">{{ Head }}</a></p>
{% endblock %}