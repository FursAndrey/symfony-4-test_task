{% extends 'base.html.twig' %}

{% block body %}

    {{ form_start(form) }}
    <div class="form-group">
        {{ form_label(form.userId, 'Список пользователей', {'label_attr': {'class': 'text-success p-3 h5' }}) }}
        {{ form_errors(form.userId) }}
        {{ form_widget(form.userId, { 'attr': {'class': 'form-control'} }) }}
        {{ form_help(form.userId) }}
    </div>
    <div class="form-group">
        <button type="submit" class="btn btn-primary">{{ sendBtn }}</button>
        <a href="{{ path('product_page') }}" class="btn btn-primary">{{ returnBtn }}</a>
    </div>
    {{ form_end(form) }}

{% endblock %}